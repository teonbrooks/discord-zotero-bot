services:
  discord-zotero-bot:
    # Use the pre-built image from GitHub Container Registry:
    image: ghcr.io/YOUR_GITHUB_USERNAME/discord-zotero-bot:latest
    # Or build locally instead:
    # build: .
    env_file:
      - .env
    restart: unless-stopped
