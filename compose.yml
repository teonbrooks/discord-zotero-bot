services:
  discord-zotero-bot:
    # Use the pre-built image from GitHub Container Registry:
    image: ghcr.io/teonbrooks/discord-zotero-bot:latest
    # Or build locally instead:
    # build: .
    env_file:
      - .env
    restart: unless-stopped
